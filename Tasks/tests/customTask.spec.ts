import * as assert from 'assert';

describe('Custom VSTS Task Tests', () => {
    beforeEach(() => {});

    it('Should run a simple unit test', () => {
        assert.equal(true, true, 'Simple test failed');
    });
});